import styled from 'styled-components';

export const CastUnorderedList = styled.ul`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  justify-items: center;
  gap: 16px;
  padding: 16px 16px;
`;
